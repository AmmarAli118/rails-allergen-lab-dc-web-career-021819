<h1> <%= @ingredient.name %> Page. </h1>

<h2> These are all the recipes you can make with <%= @ingredient.name %></h2>

<ul>
  <% @ingredient.recipes.each do |recipe| %>
    <li>
      <%= link_to recipe.name, recipe_path(recipe) %>
    </li>
  <% end %>
</ul>
